<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="hivatec.ir.hivatectoolstest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="hivatec.ir.hivatectools.helper.G"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl"
        >
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ButtonActivity" />
        <activity
            android:name=".activities.HivaAdapterActivity"
            android:label="@string/title_activity_hiva_adapter"
            android:theme="@style/HivaAppTheme.NoActionBar" />

        <!-- meysam activities -->


        <!-- Ashkan activities -->

        <activity
            android:name=".activities.RadioActivity"
            android:label="@string/title_activity_radio"
            android:theme="@style/HivaAppTheme.NoActionBar" />
        <activity
            android:name=".activities.ViewBinderActivity"
            android:label="@string/title_activity_view_binder"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.DataPickerActivity" />
        <activity android:name=".activities.LoadingActivity" />
        <activity android:name=".activities.FormActivity" />
        <activity android:name=".activities.RecyclerLoadMoreTestActivity" />
        <activity
            android:name=".activities.WebserviceActivity"
            android:label="@string/title_activity_webservice" />
        <activity
            android:name=".activities.TestNewButton"
            android:label="@string/title_activity_test_new_button"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>